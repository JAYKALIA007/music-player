import { MusicPlayer } from "@/components/MusicPlayer";

export default function Home() {
  return (
    <div className="flex justify-center items-center min-h-screen font-[family-name:var(--font-akshar)] text-[#EAF0FF]">
      <MusicPlayer />
    </div>
  );
}
